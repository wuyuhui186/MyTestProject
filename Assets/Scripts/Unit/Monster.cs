using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Monster : Unit
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello уехЩ!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
